# Backend Environment Variables
PORT=5000
NODE_ENV=development

# MongoDB Connection String (Falls back to JSON file storage in backend/data/ if unavailable)
MONGODB_URI=mongodb://127.0.0.1:27017/skywings_flightbooking

# JWT Secret Key for Authentication
JWT_SECRET=your_jwt_secret_key_here
