# Frontend Environment Variables
# If omitted or running locally with Vite proxy, requests default to /api (forwarded to localhost:5000)
# In production on a separate domain, set the backend API URL:
# VITE_API_URL=http://localhost:5000/api
